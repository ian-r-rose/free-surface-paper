all: perturb_spectrum tau_sensitivity timestep_convergence timestep_convergence_qi

.PHONY: perturb_spectrum tau_sensitivity timestep_convergence timestep_convergence_qi

perturb_spectrum:
	make -C perturb_spectrum

tau_sensitivity:
	make -C tau_sensitivity

timestep_convergence:
	make -C timestep_convergence

timestep_convergence_qi:
	make -C timestep_convergence_qi
